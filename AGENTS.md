# 项目协作约定

本目录用于实现 `PROMPT.md` 定义的浏览器口令风险评估原型。

每次开始开发前必须阅读 `开发计划.md`、`PROMPT.md` 及与当前任务相关的源码。结束前必须更新 `开发计划.md` 中对应任务的状态、验收证据、阻塞项和下一步；没有可复核证据时不得把任务标记为“已完成”。

## 不可违反的边界

- `PARD定向猜测/` 与 `RankGuess拖网猜测/` 是只读算法资产，不得修改。
- 不运行 `MC.py` 的 `main()`，不从训练入口启动推理。
- 不在日志、测试输出、缓存、提交记录或截图中写入口令、历史口令、主密钥或派生密钥。
- 算法服务只使用标准输入输出通信，不监听网络端口，也不发起出站请求。
- 两类算法指标分别解释；未知、异常、越域、未命中和未标定结果不得映射为低风险。
- KeePassXC 的算法调用必须异步，不得阻塞 GUI 主线程。
- 候选口令由用户确认网站操作成功后才能保存。

## 变更与验证

- 只提交源码、文档、配置模板和不含明文口令的可复现清单；模型权重、原始数据、缓存和构建产物均不提交。
- Python 测试优先使用 `D:\Anaconda3\envs\pytorch_cuda\python.exe`。
- 新增协议字段、错误码、阈值或模型配置时，同步更新接口文档和相应测试。
- 测试数据只引用来源文件的哈希、行号和分区，不复制真实口令到仓库。

