# 浏览器口令风险评估插件开发计划

更新时间：2026-09-22  
维护规则：开始任务前确认依赖；完成任务后填写验收证据。没有证据不得标记为已完成。

## 目标与范围

首期交付 Windows 本地竞赛演示版本，完成“解锁口令库 → 评估 → 推荐 → 复检 → 用户确认保存 → 回访填充”的完整流程。复用 KeePassXC 的 KDBX、填充机制、PasswordGenerator 和 zxcvbn；不包含账号服务器、多端同步、商店发布或模型重训。

## 状态总览

| 编号 | 优先级 | 依赖 | 状态 | 预计人日 | 交付物 | 验收条件 | 证据 | 下一步 |
| --- | --- | --- | --- | ---: | --- | --- | --- | --- |
| BASE-01 | P0 | 无 | 已完成 | 0.5 | 代码、文档、模型和数据核查记录 | 实际目录、入口和环境差异均已确认 | `PROMPT.md` 与源码核查；两份权重 SHA-256 已记录于下文 | 开始建立基线 |
| BASE-02 | P0 | BASE-01 | 进行中 | 0.5 | Git 基线与忽略规则 | 源码和计划可追踪，权重、数据、缓存和构建产物未提交 | 已创建 `.gitignore` 与 `AGENTS.md`；待提交 | 初始化仓库并提交 |
| M0-01 | P0 | BASE-02 | 阻塞 | 2–4 | MSVC/Windows SDK/vcpkg/Qt6 构建环境 | `keepassxc`、`keepassxc-proxy` 可启动并完成建库保存 | 当前未发现 Qt6、MSVC、vcpkg；`winget` 在受限环境中不可启动 | 安装工具链后配置构建 |
| M0-02 | P0 | BASE-02 | 进行中 | 0.5 | 可复现 Python 运行环境 | RankGuess、PARD 均可加载并推理 | Python 3.9.25、torch 2.8.0+cu129、CUDA 可用；缺 `torch_geometric`、`tomli` | 安装并锁定依赖 |
| M1-01 | P0 | BASE-01 | 进行中 | 1 | 算法协议和威胁模型 | 方法、状态、输入域和明文边界可核查 | 规格已由 `PROMPT.md` 固化；待生成独立接口文档 | 完成文档与契约测试 |
| M1-02 | P0 | BASE-01 | 待开始 | 1–2 | 可复现数据清单 | 固定种子、来源哈希、行号、分区、过滤统计齐全 | 数据结构已核查 | 实现抽样工具并生成清单 |
| M2-01 | P0 | M0-02,M1-01 | 待开始 | 3–5 | `algo-service/` | 真实模型冒烟测试通过，stdout 仅含协议 JSON | — | 实现适配器、服务与测试 |
| M3-01 | P0 | M0-01,M2-01 | 待开始 | 4–5 | KeePassXC 风险评估核心 | 异步调用、历史路由、等级映射和过期结果测试通过 | — | 新增 `src/riskassess/` |
| M3-02 | P1 | M3-01 | 待开始 | 2–3 | 设置、编辑界面和报告页 | 无扩展时可演示并且 GUI 不阻塞 | — | 集成宿主 UI |
| M4-01 | P1 | M1-02,M2-01 | 待开始 | 2–4 | 标定与独立验证报告 | 阈值绑定模型和推理配置；未标定时保持 UNKNOWN | — | 跑抽样数据与冻结阈值 |
| M5-01 | P1 | M3-01 | 待开始 | 4–6 | 固定版本的扩展 fork | Chrome 完整闭环、Edge 兼容验证通过 | — | 引入扩展并接动作/UI |
| M6-01 | P1 | M3-02,M4-01,M5-01 | 待开始 | 3–4 | 演示包、说明和验证报告 | 演示机可重复运行正常与异常流程 | — | 回归、安全和性能验收 |

## 已确认的实现决策

- Windows 使用 Visual Studio 2022、Windows SDK、vcpkg 和 Qt6；MSYS2 只作构建路线失败时的备选。
- 算法服务用 UTF-8 JSON Lines 通过 stdin/stdout 通信，不监听端口。
- 默认模型为 RankGuess 和 PARD；宿主 PasswordGenerator 负责默认候选生成。
- RankGuess 参考表固定 CPU、种子 `20260922`、100,000 样本，缓存绑定权重哈希和采样配置。
- PARD 初始 `batch=1`、`beam_width=100`、`top_k=200`，另测 50/100/200；最终配置变更会触发重新标定。
- 浏览器扩展使用独立 ID，在 native messaging 白名单中显式登记。
- 阈值未完成独立标定时，界面只显示原生指标及“暂无法给出强度结论”。

## 数据方案

RankGuess 从 `D:\研究生\笔记\benchmark\benchmark\test` 的 CSDN、RockYou、网易、000webhost 各抽取 3,000 条；固定种子 `20260922`，跨来源重复归入同一分区，并排除与 `180w\train_csdn.txt` 重合的内容。

PARD 从 `D:\研究生\HGN\data` 的 `csdn_test.jsonl`、`dodonew_test.jsonl`、`xsite__000webhost_2_clixsense_test.jsonl` 各抽取 1,000 对；先按 src/tgt 关联分组再划分，避免相同口令跨分区。数据没有用户 ID，因此报告只能声明口令关联隔离，不能声明严格用户级隔离。

抽样产物只记录来源文件 SHA-256、行号、固定种子、分区和过滤原因，不复制真实口令到仓库。

## 安全与验收基线

- 覆盖空库、单条和多条历史、改密、存量复检、全部历史越域、超时、服务崩溃和锁定。
- 覆盖快速输入、页面切换、库修订变化和会话变化导致的过期结果。
- 检查日志、缓存、扩展存储、网络行为和崩溃信息中不含口令或密钥明文。
- 记录模型冷启动、热请求 P50/P95、峰值内存和不同历史规模；未测数据明确写“未测”。

## 当前环境与资产指纹

- Python：3.9.25；PyTorch：2.8.0+cu129；GPU：NVIDIA GeForce RTX 5060 Ti。
- RankGuess 权重 SHA-256：`442C105BECDC849880BD1B0B3FB21477063DBC4266126D825871B3F065D1F86C`。
- PARD 权重 SHA-256：`EB0C8C5ABAF12CED38A2EC341FCC86290302E6F75677842F8F3866CFBECBC99F`。
- 已知阻塞：缺少 Qt6、MSVC、vcpkg、`torch_geometric` 和 `tomli`。

## 变更记录

| 日期 | 变更 | 原因 | 影响 |
| --- | --- | --- | --- |
| 2026-09-22 | 建立本地维护计划 | 开始执行已确认计划 | 后续状态和证据集中维护于本文件 |

