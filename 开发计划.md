# 浏览器口令风险评估插件开发计划

更新时间：2026-09-22  
维护规则：开始任务前确认依赖；结束时记录状态、证据、阻塞项和下一步。没有可复核证据不得标记为“已完成”。

## 目标与范围

首期交付 Windows 本地竞赛演示版本，覆盖“解锁口令库 → 评估 → 推荐 → 复检 → 用户确认保存 → 回访填充”。复用 KeePassXC 的 KDBX、填充机制、PasswordGenerator 和 zxcvbn；不包含账号服务器、多端同步、扩展商店发布或模型重训。

本轮已经形成可编译、可打包的演示原型。效果阈值、真实浏览器握手和人工交互验收仍单独保留，不能因为代码通过编译而标记完成。

## 状态总览

| 编号 | 优先级 | 依赖 | 状态 | 预计人日 | 交付物 | 验收条件 | 证据 | 下一步 |
| --- | --- | --- | --- | ---: | --- | --- | --- | --- |
| BASE-01 | P0 | 无 | 已完成 | 0.5 | 代码、文档、模型和数据核查记录 | 实际目录、入口和环境差异均确认 | 已核查 `PROMPT.md`、两套算法、KeePassXC、数据目录和模型指纹 | 无 |
| BASE-02 | P0 | BASE-01 | 已完成 | 0.5 | Git 基线与忽略规则 | 源码和计划可追踪；数据、权重、缓存及构建产物未提交 | 基线提交 `43633e2`；`.gitignore`、`AGENTS.md` 已建立 | 每次提交前继续检查索引内容 |
| M0-01 | P0 | BASE-02 | 待验收 | 2–4 | MSVC/Windows SDK/vcpkg/Qt6 构建环境 | 宿主、proxy 可启动，并人工完成建库和保存 | MSVC 19.44、CMake 3.29.2、Ninja 1.12.0、Qt 6.8.3；Release 构建成功；干净演示包中 CLI 退出码 0 | 在 KeePassXC GUI 人工建库、保存和锁库 |
| M0-02 | P0 | BASE-02 | 待验收 | 0.5 | 可复现 Python 环境 | RankGuess、PARD 均加载并完成基础推理 | Python 3.9.25、torch 2.8.0+cu129、torch-geometric 2.6.1、tomli 2.2.1；两模型手动加载及 `ping` 成功 | 结合用户已有结果做一次短请求复核，不跑大规模效果集 |
| M1-01 | P0 | BASE-01 | 已完成 | 1 | 算法、浏览器动作和威胁模型契约 | 方法、状态、输入域、明文边界和过期语义可核查 | `docs/algorithm-contract.md`、`docs/browser-contract.md`、`docs/threat-model.md`；静态契约检查通过 | 协议变更时同步文档和测试 |
| M1-02 | P0 | BASE-01 | 已完成 | 1–2 | 可复现数据清单 | 固定种子、来源哈希、行号、分区和过滤统计齐全 | RankGuess 12,000 条行引用；PARD 3,000 对行引用、47,617 个连通分组、排除 3,750 个训练重合配对；清单无口令明文 | 原始文件变化时重新生成 |
| M2-01 | P0 | M0-02,M1-01 | 待验收 | 3–5 | `algo-service/` | 真实模型短请求通过；stdout 仅协议 JSON；故障可恢复 | JSON Lines 服务、注册表、RankGuess/PARD 适配器、预热、串行工作线程、超时和错误状态已实现；2 项域测试及 Python 编译通过 | 用已有结果中的少量样本完成短冒烟并记录时延 |
| M3-01 | P0 | M0-01,M2-01 | 待验收 | 4–5 | KeePassXC 风险评估核心 | 异步调用、历史路由、等级映射和过期结果测试通过 | 新增 `src/riskassess/`；宿主 Release 编译通过；实现输入/口令库/会话修订校验、超时和子进程恢复 | 真实 KDBX 上人工验证空库、修改、审计、锁库和超时 |
| M3-02 | P1 | M3-01 | 进行中 | 2–3 | 设置、编辑入口和结果展示 | 无扩展时可测试核心业务且 GUI 不阻塞 | 浏览器高级设置已加入 Python、服务和配置路径；扩展风险面板已实现；宿主独立报告页尚未实现 | 根据竞赛演示需要决定是否补宿主独立报告页 |
| M4-01 | P1 | M1-02,M2-01 | 阻塞 | 2–4 | 标定与独立验证报告 | 阈值绑定模型与推理配置；未标定时保持 UNKNOWN | 用户说明已有结果位于其他文件夹，当前未提供结果路径；代码强制 `calibrated=false`，仅精确重用映射 HIGH | 导入已有结果、核对模型/配置版本后冻结阈值 |
| M5-01 | P1 | M3-01 | 待验收 | 4–6 | 固定版本扩展 fork | Chrome 完整闭环、Edge 兼容验证通过 | 固定上游提交 `85a0f0d9459e4fa550c36b5b592a4374d620d048`；独立 Chromium/Firefox ID；扩展构建成功；JS 语法通过；限定 lint 0 错误 | 在 Chrome 做真实关联、评估、填充、确认保存；Edge 复核 |
| M6-01 | P1 | M3-02,M4-01,M5-01 | 进行中 | 3–4 | 演示包、说明和验证报告 | 演示机可重复运行正常及异常流程 | `scripts/package-demo.ps1` 在干净目录执行成功；运行库无缺失；包内配置、模型引用和固定扩展公钥检查通过 | 完成人工联调、阈值导入和演示录制前检查 |

## 已实现设计

- 算法服务通过 UTF-8 JSON Lines 在 stdin/stdout 通信，不监听端口；诊断输出不会复制到 KeePassXC 日志。
- 服务提供 `ping`、`list_models`、`assess_trawling`、`assess_reuse`、`generate_candidates` 和 `shutdown`，保留原生指标与模型身份。
- KeePassXC 使用 `QProcess` 异步调用服务。普通请求为 3 秒服务预算和 1.5 秒宿主余量，超时、进程退出、配置缺失和非法输出均保持未知状态。
- 历史口令排除回收站、过期项和引用值，按最近修改排序，去重后最多使用 5 条；审计排除自身，修改场景保留当前旧口令。
- 推荐候选由 KeePassXC `PasswordGenerator` 生成。有历史时先做重用检查，再做拖网检查。阈值未冻结时结果为 `REVIEW_REQUIRED`，不会显示 `QUALIFIED`。
- 扩展后台从活动 HTTP(S) 标签页生成来源，只接受扩展自身页面发起风险动作。候选只保存在当前标签页后台内存，十分钟过期；内容脚本不接收历史集合。
- 只有用户勾选“我确认网站已成功修改口令”后，扩展才调用原有 `set-login` 更新所选 KeePassXC 条目。
- Chromium 固定 ID 为 `ijlckofhohjbbifcfhpiglkmfndaaeol`；Firefox ID 为 `they-know-your-passwords@local.demo`，均写入宿主 Native Messaging 白名单。

## 数据与标定

RankGuess 清单来自 `D:\研究生\笔记\benchmark\benchmark\test` 的 CSDN、RockYou、网易和 000webhost，每份 3,000 条，种子 `20260922`。跨来源重复保持同一分区，并排除与已知 CSDN 训练文件重合的内容。

PARD 清单来自 `D:\研究生\HGN\data` 的 CSDN、Dodonew 和 000webhost→Clixsense，每份 1,000 对。按 src/tgt 连通分组后划分，避免相同口令跨分区，并排除已知训练重复配对。数据没有用户 ID，因此只能声明口令关联隔离。

用户已说明效果结果位于另一文件夹。本轮不重复跑大规模测试集；在结果路径和模型配置版本核对前，不导入阈值，也不将 `calibrated` 改为真。

## 当前验证记录

- Python 契约单元测试：2/2 通过。
- Python 语法编译：通过。
- 跨组件静态契约：`integration-contract=PASS`。
- KeePassXC Release：`KeePassXC.exe`、`keepassxc-proxy.exe`、`keepassxc-cli.exe` 编译成功。
- 扩展：Firefox/Chromium 打包成功；4 个相关脚本 `node --check` 通过；限定 ESLint 为 0 错误、11 个协议 snake_case 警告。
- 干净本地包：`D:\tmp\TheyKnowDemoCheck` 生成成功，必要运行库缺失数为 0，CLI 退出码为 0，服务配置和 Chromium 固定公钥检查通过。
- 完整真实模型冒烟因冷启动超过 90 秒已停止，未运行非精确 PARD beam 搜索；这是按用户要求控制效果测试投入后的已知限制。

## 阻塞项与下一步

1. 需要用户已有结果的目录路径，才能核对模型哈希、beam/top-k/sample 配置并冻结阈值。
2. 需要在可交互 Windows 桌面完成 KeePassXC 建库保存和 Chrome Native Messaging 全闭环；Edge 随后复核同一 Chromium 包。
3. 真实握手通过后补充冷/热启动、P50/P95、峰值内存和异常演示证据。

## 变更记录

| 日期 | 变更 | 原因 | 影响 |
| --- | --- | --- | --- |
| 2026-09-22 | 建立本地维护计划和 Git 基线 | 开始执行已确认计划 | 任务状态和证据集中维护于本文件 |
| 2026-09-22 | 实现算法服务、抽样清单、KeePassXC 异步宿主和扩展风险面板 | 形成首期本地原型 | M1 完成；M2、M3、M5 进入待验收 |
| 2026-09-22 | 将效果集重跑降为少量复核 | 用户已有独立结果，要求避免重复耗时 | M4 等待导入既有结果；未标定状态保持 UNKNOWN |
| 2026-09-22 | 增加 ASCII 源码联接构建路线和可重复打包脚本 | Ninja 无法可靠扫描中文源码路径；安装包需补齐 vcpkg 运行库 | 干净演示目录可生成并通过静态完整性检查 |
